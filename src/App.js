import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { SearchSong, Song, Splash } from './pages'

const App = () => {
  return <Song/>;
};

export default App

const styles = StyleSheet.create({})
