import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { SearchSong, Splash } from './pages'

const App = () => {
  return <SearchSong/>;
};

export default App

const styles = StyleSheet.create({})
